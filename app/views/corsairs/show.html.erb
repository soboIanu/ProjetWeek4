<h6><%= link_to "Editer", edit_corsair_path(@corsair.id), method: "get" %></h6>
<h1>Bienvenue <%= @corsair.first_name.capitalize%> !</h1>
<h2>Tu as <%= @corsair.age%> ans !</h2><br>

<div class="div_show">
  <p><b><%= @corsair.first_name.capitalize%> préfère le: </b><br> <%=@corsair.likeness%></p> <!-- William style -->

<% if @corsair.bio != "" && @corsair.bio != nil %>
  <p><b>Voici la bio de <%= @corsair.first_name.capitalize%></b> : <br><%=@corsair.bio%></p> <!-- William style -->
<% else %>
	<p><%=@corsair.first_name.capitalize%> n'a pas encore renseigné sa vie !</p>
<% end %>

<% if @corsair.slack_handle != "" && @corsair.slack_handle != nil %>
	<p><b>Voici le pseudo Slack de <%=@corsair.first_name.capitalize%></b> :<br> <%= link_to @corsair.slack_handle.to_s, "https://thehackingproject.slack.com/team/" + @corsair.slack_handle.to_s %></p>
<% else %>
	<p><%=@corsair.first_name.capitalize%> n'a pas encore renseigné son pseudo Slack !</p>
<% end %>

<% if @corsair.github_handle != "" && @corsair.github_handle != nil %>
	<p><b>Voici le pseudo Github de <%=@corsair.first_name.capitalize%></b> : <br><%= link_to @corsair.github_handle.to_s, "https://github.com/" + @corsair.github_handle.to_s %></p>
  <% else %>
	<p><%=@corsair.first_name.capitalize%> n'a pas encore renseigné son pseudo Github !</p>
<% end %>
</div>
