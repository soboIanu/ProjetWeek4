<h1>Liste de l'équipage</h1>
<ol>
  <% @corsairs.each do |c| %>
  <li>Corsaire <%= c.first_name %> : <%= c.age %> ans <%= link_to "Voir le profil", corsair_path(c.id) %></li>
  <% end %>
</ol>
<%= button_to "Créer un corsaire", "new_corsair_path", method: "GET", class: "btn-lg" %>
